# CSV 重绘 PNG 废弃归档说明

本目录保存早期从 ILA CSV 辅助重绘得到的 PNG 图和相关摘要文件。

这些图片不是 Vivado Hardware Manager / ILA GUI 的真实截图，仅用于早期离线辅助可视化。当前正式报告不再将这些 CSV 重绘 PNG 作为主证据。

当前证据优先级为：

```text
真实 Vivado GUI 截图 > 原始 ILA CSV/log > 辅助脚本统计 > CSV 重绘 PNG
```

原始 CSV/log 未删除，真实 Vivado GUI 截图仍保留在：

```text
docs/images/
```

